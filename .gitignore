# Dependencies
node_modules/
vendor/

# Test / tooling artifacts
.phpunit.result.cache
test-results/
playwright-report/
blob-report/
coverage/

# Env
.env
.env.local
e2e/.env

# Playwright local auth (session cookies — never commit)
.auth/
build/
